package com.example.demo.model.appuser;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Profile;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.TestPropertySource;

import static org.junit.jupiter.api.Assertions.*;


//@DataJpaTest

class AppUserRepoTest {
/*

    @Autowired
     AppUserRepo appUserRepos;
    @Test
    public void TestRepo(){
    AppUser appUser = new AppUser("NAme","name","username@domain.de");
    appUserRepos.save(appUser);
    boolean exist  = appUserRepos.findByEmail("username@domain.de").isPresent();
    assertTrue(exist);
}
@Test
public void emailValTest(){
    AppUser appUser = new AppUser("NAme","name","email");
    appUserRepos.save(appUser);
    boolean exist  = appUserRepos.findByEmail("email").isPresent();
    assertFalse(exist);
}
*/

}
